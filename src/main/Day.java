package main;

public enum Day {
    SUNDAY,
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY,
    SATURDAY

    // equals and hashCode is final in Enum
    // so cannot be overridden
}
